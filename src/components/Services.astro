---
const services = [
  {
    title: "Diseño UI/UX",
    category: "Interfaces intuitivas y experiencias memorables",
    image: "/images/service1.jpg",
    link: "/services/service1"
  },
  {
    title: "Branding",
    category: "Identidad visual única y coherente",
    image: "/images/service2.png",
    link: "/services/service2"
  },
  {
    title: "Desarrollo Web",
    category: "Sitios web modernos y responsivos",
    image: "/images/service3.jpg",
    link: "/services/service3"
  },
  {
    title: "Marketing Digital",
    category: "Estrategias efectivas para tu negocio",
    image: "/images/service4.jpg",
    link: "/services/service4"
  }
];
---

<section class="services">
  <div class="container">
    <header class="section-header">
      <h2 class="fade-in">Nuestros Servicios</h2>
      <a href="/services" class="view-all">Ver todo</a>
    </header>

    <div class="services-grid">
      {services.map((service, index) => (
        <a href={service.link} class="service-card" data-index={index}>
          <div class="service-image">
            <img src={service.image} alt={service.title} loading="lazy" />
          </div>
          <div class="service-info">
            <h3>{service.title}</h3>
            <p>{service.category}</p>
          </div>
        </a>
      ))}
    </div>

    <div class="manifesto">
      <p class="fade-in">Creamos soluciones únicas, historias y experiencias que las audiencias aman.</p>
      <p class="fade-in">Porque las soluciones que aman no solo inspiran. Construyen lealtad, entregan valor y generan crecimiento.</p>
      <p class="fade-in">Usamos la creatividad para servir a tus ambiciones comerciales.</p>
    </div>
  </div>
</section>

<style>
  .services {
    padding: var(--spacing-xl) 0;
  }

  .section-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: var(--spacing-lg);
  }

  .view-all {
    font-size: 1rem;
    font-weight: 500;
    position: relative;
    padding-bottom: 4px;
    opacity: 0;
    animation: fadeIn 0.8s ease-out forwards;
    animation-delay: 0.3s;
  }

  .view-all::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 1px;
    background-color: currentColor;
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 0.6s var(--ease-out-expo);
  }

  .view-all:hover::after {
    transform: scaleX(1);
    transform-origin: left;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--spacing-lg);
    margin-bottom: var(--spacing-xl);
  }

  .service-card {
    position: relative;
    overflow: hidden;
  }

  .service-image {
    aspect-ratio: 16/9;
    position: relative;
  }

  .service-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transform: scale(1);
    transition: transform 0.6s var(--ease-out-expo);
  }

  .service-card:hover .service-image img {
    transform: scale(1.05);
  }

  .service-info {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: white;
    z-index: 1;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s ease, visibility 0.3s ease;
    backdrop-filter: blur(10px); /* Glass blur effect */
    padding: 10px;
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.2); /* Semi-transparent background */
  }

  .service-card:hover .service-info {
    opacity: 1;
    visibility: visible;
  }

  .manifesto {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
    font-size: 1.5rem;
    line-height: 1.4;
  }

  .manifesto p {
    margin-bottom: var(--spacing-md);
    opacity: 0;
    transform: translateY(20px);
    animation: slideUp 0.8s var(--ease-out-expo) forwards;
  }

  .manifesto p:nth-child(1) { animation-delay: 0.8s; }
  .manifesto p:nth-child(2) { animation-delay: 1s; }
  .manifesto p:nth-child(3) { animation-delay: 1.2s; }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 768px) {
    .services-grid {
      grid-template-columns: 1fr;
      gap: var(--spacing-md);
    }

    .manifesto {
      font-size: 1.2rem;
    }
  }
</style>

<script>
  // Opcional: Reiniciar animaciones cuando el componente entra en vista
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const target = entry.target as HTMLElement;
        target.style.animation = '';
        target.offsetHeight; // Trigger reflow
        target.style.animation = '';
        observer.unobserve(target);
      }
    });
  }, {
    threshold: 0.1
  });

  document.querySelectorAll('.fade-in, .service-card, .manifesto p').forEach((el) => {
    observer.observe(el);
  });
</script>
